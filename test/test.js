'use strict';
var assert = require('assert');
var officeClippy = require('../');

describe('office-clippy node module', function () {
    it('must have at least one test', function () {
        officeClippy();
        assert(true, 'I am writing tests ok? -_-');
    });
});